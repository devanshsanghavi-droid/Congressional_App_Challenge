/**
 * Public entry point for the extraction cascade.
 *
 * -------------------------------------------------------------------------
 * AUTHORSHIP: this directory is written by Devansh (CLAUDE.md, SPEC.md §13).
 * This file was created by Claude as build plumbing only — `tsc -p
 * src/extraction` errors out on a config with no input files, so the island's
 * typecheck could not run until at least one .ts file existed here. It holds
 * no parsing logic, no templates, no heuristics, and no type design. Replace
 * it with real exports in Phase 2.
 * -------------------------------------------------------------------------
 *
 * Everything in this directory is pure TypeScript with no platform access —
 * see ./README.md for what that means and why it is enforced three different
 * ways.
 */

export {};
